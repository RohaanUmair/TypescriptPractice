const arr: string[] = ['Hello', 'World', 'TypeScript', 'Coding'];
const lengths: number [] = arr.map((word) => word.length);   //saves length of each string in lengths variable

console.log(arr);
console.log(lengths);
