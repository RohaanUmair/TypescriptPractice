var arr = ['Hello', 'World', 'TypeScript', 'Coding'];
var lengths = arr.map(function (word) { return word.length; }); //saves length of each string in lengths variable
console.log(arr);
console.log(lengths);
