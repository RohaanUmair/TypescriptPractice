// Define an array of favorite movies and iterate through each using for...of loop
let favMovies: string[] = ['Interstellar', 'Spiderman', 'Harry Potter'];

for (const movie of favMovies) {
    console.log(movie);
}