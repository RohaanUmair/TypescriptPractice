function convertCase(str) {
    var upperCase = str.toUpperCase(); //converts to upper case
    var lowerCase = str.toLowerCase(); //converts to lower case
    console.log("\n    Upper case: ".concat(upperCase, "\n    Lower case: ").concat(lowerCase, "\n    "));
}
convertCase('Hello World!');
