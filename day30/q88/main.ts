function roundNum(num: number): number {
    return Math.round(num);   //rounds the number to the nearest whole number
}

console.log(roundNum(3.7));   //outputs 4
console.log(roundNum(2.3));   //outputs 2